# Database configuration
development:
  adapter: postgresql
  host: localhost
  port: 5432
  database: omnilexis_dev
  username: omnilexis
  password: omnilexis

production:
  adapter: postgresql
  host: ${DB_HOST}
  port: ${DB_PORT}
  database: ${DB_NAME}
  username: ${DB_USER}
  password: ${DB_PASSWORD}
